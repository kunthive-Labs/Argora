__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# Scraped data + generated SQL = local only (PII; never push by mistake).
# The .gitkeep files keep the folders tracked; the contents stay out.
data/raw/*
data/leads/*
data/sql/*
!data/raw/.gitkeep
!data/leads/.gitkeep
!data/sql/.gitkeep
